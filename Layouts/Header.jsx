import { Navbar, Hero } from '../Sections/index';

const Header = () => {
    return (
        <header>
            <Navbar />
            <Hero />
        </header>
    );
};

export default Header;
